{
  "name": "cloudformation-serverless-v3",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "deploy": "sls deploy --verbose",
    "package": "sls package --verbose"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.312.0",
    "@aws-sdk/lib-dynamodb": "^3.312.0",
    "@aws-sdk/smithy-client": "^3.310.0",
    "@aws-sdk/types": "^3.310.0"
  },
  "devDependencies": {
    "@serverless/typescript": "^3.29.0",
    "@tsaws/cloudformation": "workspace:*",
    "@types/node": "^18.14.6",
    "esbuild": "^0.17.11",
    "serverless": "^3.30.1",
    "serverless-esbuild": "^1.39.0",
    "typescript": "~4.9.5"
  },
  "eslintConfig": {
    "rules": {
      "turbo/no-undeclared-env-vars": "off"
    }
  }
}
