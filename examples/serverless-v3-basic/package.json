{
  "name": "serverless-v3-basic",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "deploy": "sls deploy --verbose"
  },
  "dependencies": {
    "@tsaws/lambda": "workspace:*",
    "zod": "^3.21.0"
  },
  "devDependencies": {
    "@types/node": "^18.14.6",
    "esbuild": "^0.17.11",
    "serverless": "^3.28.1",
    "serverless-esbuild": "^1.39.0",
    "serverless-prune-plugin": "^2.0.2",
    "typescript": "~4.9.5"
  },
  "eslintConfig": {
    "rules": {
      "turbo/no-undeclared-env-vars": "off"
    }
  }
}
